apiVersion: v1
kind: Service

metadata:
  name: retailkart-service

spec:
  type: NodePort

  selector:
    app: retailkart

  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30081
