apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "retailkart.fullname" . }}-test-connection"
  labels:
    {{- include "retailkart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "retailkart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
