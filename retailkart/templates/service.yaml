apiVersion: v1
kind: Service
metadata:
  name: {{ include "retailkart.fullname" . }}
  labels:
    {{- include "retailkart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "retailkart.selectorLabels" . | nindent 4 }}
