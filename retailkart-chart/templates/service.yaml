apiVersion: v1
kind: Service
metadata:
  name: {{ include "retailkart-chart.fullname" . }}
  labels:
    {{- include "retailkart-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "retailkart-chart.selectorLabels" . | nindent 4 }}
